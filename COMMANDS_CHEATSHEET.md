# নিজের প্র্যাকটিসের জন্য — কপি-পেস্ট কমান্ড শিট

এই কমান্ডগুলো **ঠিক এই অর্ডারে, একটার পর একটা** সিমুলেটরের ইনপুট বক্সে পেস্ট করে Enter দিন। এগুলো সিমুলেটরে যে ডেটা বসানো আছে (DAC-DXB রুট) তার সাথে মিলিয়ে বানানো, তাই কাজ করার কথা।

## ধাপ ১ — ফ্লাইট সার্চ
```
AN22NOVDACDXB
```
৬টা লাইন দেখাবে। **লাইন ২ বা ৩** সবচেয়ে সহজে sell হবে (এগুলোতে "phantom" সমস্যা নেই)।

## ধাপ ২ — সিট সেল করা
```
SS1Y2
```
(মানে: ১টা সিট, Y ক্লাস, লাইন ২ থেকে)। সফল হলে স্ট্যাটাস **DK1** দেখাবে।

> **ট্রাই করে দেখুন এটাও:** `SS1K1` দিলে কী হয় — লাইন ১ "phantom" হওয়ায় "NOT AVAILABLE AND WAITLIST CLOSED" এরর আসবে, এটা স্বাভাবিক (বাগ না)।

## ধাপ ৩ — যাত্রীর নাম
```
NM1RAHMAN/MD HAFIZUR MR
```
নাম যোগ হওয়ার পর `*** NHP ***` রিমাইন্ডার আসবে — মানে ফোন নম্বর এখনো বাকি।

## ধাপ ৪ — ফোন/কন্টাক্ট ফিল্ড
```
AP WINGS FLY TOURS AND TRAVELS REF HAFIZ 01618000488
```

## ধাপ ৫ — টিকেটিং টাইম লিমিট
```
TKTL
```

## ধাপ ৬ — রিসিভড ফিল্ড (ওয়ার্নিং দেখাবে, এটাই স্বাভাবিক)
```
RFH;ER
```

## ধাপ ৭ — শেষ করা (এন্ড ট্রানজেকশন)
```
ER
```
এই কমান্ডের পর PNR ফাইনাল হবে, একটা রেকর্ড লোকেটর পাবেন, আর সব সেগমেন্টের স্ট্যাটাস **DK1 থেকে HK1**-এ বদলে যাবে।

## ধাপ ৮ — ইতিনেরারি ইমেইল পাঠানো (সিমুলেটেড)
```
IEP-EML-yourname@example.com
```

## ধাপ ৯ — PNR আবার খোলা (রিট্রিভ)
রেকর্ড লোকেটর দিয়ে (ধাপ ৭-এ যেটা পেয়েছেন সেটা বসান):
```
RT[আপনার লোকেটর এখানে বসান]
```
অথবা নাম দিয়ে:
```
RT/RAHMAN/MD HAFIZUR
```

## ধাপ ১০ — PNR ক্যান্সেল করা
```
XI
```

---

## অন্যান্য রুট দিয়ে প্র্যাকটিস

- **QR রাউন্ড ট্রিপ (ভেরিফায়েড ডেটা):** `AN19MARDACDOH` → sell → তারপর `AN19MARDOHDXB` → sell → `AN25MARDXBDOH` → sell → `AN25MARDOHDAC` → sell — এভাবে ৪ লেগের একটা রিটার্ন PNR বানানো যায়
- **ভুল সিনট্যাক্স টেস্ট করতে:** `SS3KK2` দিয়ে দেখুন — "CHECK CLASS OF SERVICE" আসবে (ক্লাস একটাই অক্ষর হতে হয়)
- **অন্য এয়ারপোর্ট:** ডান পাশের প্যানেলে যে এয়ারপোর্ট কোডগুলো আছে (JED, RUH, BKK, ইত্যাদি) সেগুলো দিয়েও `AN[date]DAC[code]` ফরম্যাটে সার্চ করতে পারবেন — এগুলো জেনেরিক প্র্যাকটিস ডেটা, রিয়েল স্ক্রিনশট দিয়ে যাচাই করা না

## মনে রাখার জন্য

- তারিখ ফরম্যাট: `DDMMM` (যেমন `22NOV`, `19MAR`)
- সেল কমান্ড ফরম্যাট: `SS[সংখ্যা][ক্লাস-১অক্ষর][লাইন]`
- একটা নতুন সেশন শুরু করতে চাইলে সিমুলেটরে "Reset session" বাটনে চাপুন
